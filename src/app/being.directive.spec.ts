import { BeingDirective } from './being.directive';

describe('BeingDirective', () => {
  it('should create an instance', () => {
    const directive = new BeingDirective();
    expect(directive).toBeTruthy();
  });
});
